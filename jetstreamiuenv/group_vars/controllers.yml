---

group_packages:
  - git
  - python-virtualenv

jetstream_scratch0_device: /dev/sdb

directories:
  - path: /jetstream
  - path: /var/lib/slurm
    mode: "0755"

links:
  - path: /jetstream/scratch0
    src: /scratch0
    force: yes
