http:
  routers:
    stats-rtr:
      rule: "Host(`stats.galaxyproject.eu`)"
      service: stats
      entryPoints:
        - websecure
        - web
      middlewares:
        - stats-compress
        - https-redirect
      tls:
        certResolver: "route53"
        domains:
          - main: "stats.galaxyproject.eu"
