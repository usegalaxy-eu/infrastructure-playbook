server {

    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;

    server_name {{ inventory_hostname }};

    location / {
        proxy_pass                 http://127.0.0.1:15672;
    }
}
